spring:
  application:
    name: res-with-spring-boot-java-bruno
  datasource:
    url: jdbc:postgresql://34.59.151.36:5432/noname?sslmode=disable
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
